# Project Rules & Guidelines

## UI & Loading State Rules
- **Loading Skeletons Required by Default**: Whenever new features, routes, or components are added to the website, always integrate UI loading skeletons by default (using `Skeleton` components and Next.js `loading.tsx` route conventions).
